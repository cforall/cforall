#include <fstream.hfa>
#include <stdio.h>

enum(char *) IntEnum {
    zero="zero",
    one="one"
};

int main() {
    sout | "zero: "| zero | ", one: " | one;
    printf("zero: %d, one: %d\n", zero, one);
    return 0;
}